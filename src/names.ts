// A name means one concept in every ecosystem, so a key carries no ecosystem.
// Claiming a name is free. Renaming or removing one breaks every address already written with it.
export const names = {
  'string/truncate': 'Shortens a string to at most a given length.',
}
